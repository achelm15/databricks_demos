# Core HTTP client — the demo is plain `requests` against rostr.cc + Sheets v4.
requests>=2.31.0
python-dotenv>=1.0.0

# Google Sheets read/update via the v4 REST API directly (AuthorizedSession).
# - Local dev uses gcloud Application Default Credentials (already on your laptop).
# - Production job uses a GCP service-account JSON via GOOGLE_APPLICATION_CREDENTIALS.
google-auth>=2.30.0

# Optional — only needed if PERSIST_TO_UC=true.
databricks-connect>=18.0.0,<18.1.0
databricks-sdk>=0.20.0
